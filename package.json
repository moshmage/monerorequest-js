{
  "name": "@monerorequest-js",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "bundle:request": "npm run bundle --workspace packages/paymentrequest",
    "bundle:paywith": "npm run bundle --workspace packages/paywithmonero"
  }
}
